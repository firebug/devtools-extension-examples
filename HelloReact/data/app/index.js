/* See license.txt for terms of usage */

define(function(require) {

// Application modules
var { renderTabbedBox } = require("app/tabs");
var { renderDropdownButton } = require("app/dropdown");

// Rendering
renderDropdownButton();
renderTabbedBox();

});
